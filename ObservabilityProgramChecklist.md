| Phase                                    | Scope                                                                  | Status        | % Complete | Key Components / Tasks                                                                                                 | Primary Files / Artifacts                                                        | Notes                                                             |
| ---------------------------------------- | ---------------------------------------------------------------------- | ------------- | ---------- | ---------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- | ----------------------------------------------------------------- |
| **O1 – Prometheus Exporter Integration** | Expose `/metrics` for .NET API, Python vector-index, Postgres exporter | ⏳ Not Started | 0%         | Add `UseMetricServer()`, `UseHttpMetrics()`; Add FastAPI Prometheus middleware; Add Postgres exporter container        | `docker-compose.observability.yml`, `prometheus.yml`, `Program.cs` modifications | API vẫn chỉ expose raw metrics; không tính toán gì trong backend. |
| **O2 – Grafana Setup & Dashboards**      | Deploy Grafana + build core dashboards                                 | ⏳ Not Started | 0%         | Provision Grafana datasource for Prometheus; Create dashboards for API latency, error rate, DB load, Qdrant throughput | `grafana/datasources/*.yaml`, `grafana/dashboards/*.json`                        | Dashboard tự động load qua provisioning.                          |
| **O3 – System Alerting (Alertmanager)**  | Implement Prometheus alert rules + Alertmanager                        | ⏳ Not Started | 0%         | Define rule groups: high latency, error spike, Qdrant backlog, Redis saturation, Postgres slow queries                 | `alertmanager.yml`, `prometheus-alert-rules.yml`                                 | Đây là alert **hệ thống**, không liên quan moderation.            |
| **O4 – Logs / Tracing (Optional)**       | Add Loki or OpenTelemetry for deeper diagnostics                       | ⏳ Not Started | 0%         | Add OTEL collector container; integrate with ASP.NET & FastAPI; enable traces + logs                                   | `otel-collector-config.yml`, logging configs                                     | Chỉ làm nếu hệ thống cần tracking sâu.                            |
| **O5 – Production Hardening**            | Retention tuning, storage optimization, cleanup                        | ⏳ Not Started | 0%         | Configure retention, storage backends, scrape intervals, rate limits                                                   | Prometheus storage settings, Grafana provisioning                                | Optional cho deployment thật.                                     |
